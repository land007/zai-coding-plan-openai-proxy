[package]
name = "zai-coding-plan-openai-proxy"
version = "0.1.0"
edition = "2024"

[dependencies]
base64 = "0.22"
openssl = { version = "0.10", features = ["vendored"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
codegen-units = 1
strip = true
